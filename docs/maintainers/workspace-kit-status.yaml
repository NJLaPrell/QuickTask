# Maintainer-only: execution handoff for workspace kit work (multi-session agents).
# Do not link from product README.md (see ROADMAP.md documentation boundary).
#
# Agent session loop: read this file → align with ROADMAP.md → execute TASKS.md items
# tagged [workspace-kit] → update this file on exit.

schema_version: 1

# Roadmap "Phases (full plan)" — integer 0-6
current_kit_phase: 5

# ISO date (YYYY-MM-DD) of last intentional update to this file
last_updated: "2026-03-23"

# One line: what the next session should prioritize (agent or human fills)
active_focus: "T169 complete — execute T170 Phase 5 promotion evidence and state update"

# Human-required stops; agent must not "resolve" these without user
blockers: []

# Ordered hints for the next agent run (clear when done)
next_agent_actions:
  - "Run T170 promotion closeout using Phase 5 matrix checks (including improvement log validation and release-readiness evidence)"
  - "If T170 evidence passes, advance `current_kit_phase` to 6 and set extraction execution as active focus"
  - "Prepare Phase 6 extraction execution task chain for single-package split path"

# Optional: 2-5 bullets for the following session
last_session_summary:
  - "Completed T169 first release-cycle evidence pass using machine-readable improvement log and deterministic summary generation."
  - "Resolved baseline friction record and captured release-cycle evidence record in `.workspace-kit/improvement-log.json`."
  - "Validated improvement engine in release-readiness flow (`release:prepare`) with zero new findings and READY handoff state."
  - "Set next execution focus to T170 for 5 -> 6 promotion closeout evidence."

# Open roadmap decisions still blocking implementation (mirror ROADMAP open decisions)
pending_decisions:
  - package_name_and_scope
  - copilot_vs_cursor
  - merge_strategy_on_upgrade

# Metrics baseline (ROADMAP: update at least once per kit semver minor or phase promotion)
metrics:
  updated_at: "2026-03-23"
  source: "manual"
  notes: "T169 complete: first release-cycle evidence pass recorded for machine-readable improvement engine; Phase 5 promotion closeout pending."
  time_to_doctor_minutes: 0.03
  cold_start_success_rate_pct: 100
  upgrade_success_rate_pct: null
  recurring_friction_themes_count: 1
