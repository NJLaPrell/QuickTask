# Maintainer-only: execution handoff for workspace kit work (multi-session agents).
# Do not link from product README.md (see ROADMAP.md documentation boundary).
#
# Agent session loop: read this file → align with ROADMAP.md → execute TASKS.md items
# tagged [workspace-kit] → update this file on exit.

schema_version: 1

# Roadmap "Phases (full plan)" — integer 0-6
current_kit_phase: 5

# ISO date (YYYY-MM-DD) of last intentional update to this file
last_updated: "2026-03-23"

# One line: what the next session should prioritize (agent or human fills)
active_focus: "T168 complete — execute T169 first release-cycle evidence pass for improvement engine"

# Human-required stops; agent must not "resolve" these without user
blockers: []

# Ordered hints for the next agent run (clear when done)
next_agent_actions:
  - "Run T169 release-cycle evidence pass using machine-readable improvement log + deterministic summary"
  - "Capture release:prepare evidence that includes workspace-kit improvement checks"
  - "Plan and execute dedicated 5 -> 6 promotion closeout task once release-cycle evidence is recorded"

# Optional: 2-5 bullets for the following session
last_session_summary:
  - "Completed T168 by implementing a schema-backed workspace-kit improvement log and deterministic generated summary artifacts."
  - "Added validation/generation scripts with tests and wired improvement checks into workflow-contract checks, release readiness, and CI."
  - "Updated Phase 5/6 roadmap and maintainer docs to use contract-first, configurable improvement policy."
  - "Set next execution focus to T169 for first release-cycle evidence capture before Phase 6 extraction execution."

# Open roadmap decisions still blocking implementation (mirror ROADMAP open decisions)
pending_decisions:
  - package_name_and_scope
  - copilot_vs_cursor
  - merge_strategy_on_upgrade

# Metrics baseline (ROADMAP: update at least once per kit semver minor or phase promotion)
metrics:
  updated_at: "2026-03-23"
  source: "manual"
  notes: "Phase 5 kickoff complete: machine-readable improvement log and deterministic summary checks integrated; release-cycle evidence pass pending."
  time_to_doctor_minutes: 0.03
  cold_start_success_rate_pct: 100
  upgrade_success_rate_pct: null
  recurring_friction_themes_count: 0
