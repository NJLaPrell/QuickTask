# Maintainer-only: execution handoff for workspace kit work (multi-session agents).
# Do not link from product README.md (see ROADMAP.md documentation boundary).
#
# Agent session loop: read this file → align with ROADMAP.md → execute TASKS.md items
# tagged [workspace-kit] → update this file on exit.

schema_version: 1

# Roadmap "Phases (full plan)" — integer 0-6
current_kit_phase: 6

# ISO date (YYYY-MM-DD) of last intentional update to this file
last_updated: "2026-03-23"

# One line: what the next session should prioritize (agent or human fills)
active_focus: "Phase 6 complete — workspace-kit roadmap delivered; extraction repo execution can proceed as follow-up when requested"

# Human-required stops; agent must not "resolve" these without user
blockers: []

# Ordered hints for the next agent run (clear when done)
next_agent_actions:
  - "No open Phase 0-6 roadmap tasks remain; keep workspace-kit in maintenance mode unless new scoped work is requested"
  - "If extraction execution is requested, use `docs/maintainers/workspace-kit-phase6-extraction-plan.md` as canonical cutover sequence"
  - "Preserve packaged-consumer regression gate (`pnpm workspace-kit:phase6:consumer-check`) in CI/release/readiness flows"

# Optional: 2-5 bullets for the following session
last_session_summary:
  - "Completed T171 Phase 6 kickoff and mapped extraction closeout work into T172/T173/T174."
  - "Published canonical Phase 6 extraction and consumer-boundary docs, and recorded the `git subtree split` history strategy decision."
  - "Implemented packaged-kit consumer regression command and enforced it in CI, release, release-candidate, and release-readiness checks."
  - "Closed T174 with full validation evidence and marked workspace-kit roadmap phases complete through Phase 6."

# Open roadmap decisions still blocking implementation (mirror ROADMAP open decisions)
pending_decisions:
  - package_name_and_scope
  - copilot_vs_cursor
  - merge_strategy_on_upgrade

# Metrics baseline (ROADMAP: update at least once per kit semver minor or phase promotion)
metrics:
  updated_at: "2026-03-23"
  source: "manual"
  notes: "Phase 6 complete: extraction plan and packaged-consumer boundary gates are in place; roadmap execution delivered through final phase."
  time_to_doctor_minutes: 0.03
  cold_start_success_rate_pct: 100
  upgrade_success_rate_pct: null
  recurring_friction_themes_count: 1
