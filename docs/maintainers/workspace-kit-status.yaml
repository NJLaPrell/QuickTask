# Maintainer-only: execution handoff for workspace kit work (multi-session agents).
# Do not link from product README.md (see ROADMAP.md documentation boundary).
#
# Agent session loop: read this file → align with ROADMAP.md → execute TASKS.md items
# tagged [workspace-kit] → update this file on exit.

schema_version: 1

# Roadmap "Phases (full plan)" — integer 0-6
current_kit_phase: 6

# ISO date (YYYY-MM-DD) of last intentional update to this file
last_updated: "2026-03-23"

# One line: what the next session should prioritize (agent or human fills)
active_focus: "Extraction follow-through complete — Workflow Cannon repo bootstrap and first npm publish verified"

# Human-required stops; agent must not "resolve" these without user
blockers: []

# Ordered hints for the next agent run (clear when done)
next_agent_actions:
  - "No open extraction follow-through blockers remain; keep workspace-kit in maintenance mode unless new work is requested"
  - "Keep packaged-consumer regression gate (`pnpm workspace-kit:phase6:consumer-check`) enforced in QuickTask CI/readiness"
  - "Use Workflow Cannon repository as canonical package home for future workspace-kit release changes"

# Optional: 2-5 bullets for the following session
last_session_summary:
  - "Resolved npm human gates by setting `NPM_TOKEN` secret and confirming `@workflow-cannon` scope ownership."
  - "Ran Workflow Cannon `Publish NPM` workflow successfully (`run_id: 23463225397`)."
  - "Verified package is live on npm as `@workflow-cannon/workspace-kit@0.1.0` with `latest` tag."
  - "Closed T177 extraction blocker and marked extraction follow-through complete."

# Open roadmap decisions still blocking implementation (mirror ROADMAP open decisions)
pending_decisions: []

# Metrics baseline (ROADMAP: update at least once per kit semver minor or phase promotion)
metrics:
  updated_at: "2026-03-23"
  source: "manual"
  notes: "Extraction follow-through complete: Workflow Cannon repository bootstrap and first npm publish succeeded (`@workflow-cannon/workspace-kit@0.1.0`)."
  time_to_doctor_minutes: 0.03
  cold_start_success_rate_pct: 100
  upgrade_success_rate_pct: null
  recurring_friction_themes_count: 1
