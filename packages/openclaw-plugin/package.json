{
  "name": "quicktask-openclaw",
  "version": "0.3.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NJLaPrell/QuickTask.git"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "test": "pnpm run build && node --test test/**/*.test.mjs",
    "package:artifact": "pnpm run build && node ../../scripts/package-openclaw-artifact.mjs"
  },
  "dependencies": {
    "@quicktask/core": "file:../core"
  },
  "files": [
    "dist",
    "package.json",
    "CHANGELOG.md"
  ]
}
