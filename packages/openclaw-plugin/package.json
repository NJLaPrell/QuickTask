{
  "name": "quicktask-openclaw",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist",
    "test": "pnpm run build && node --test test/**/*.test.mjs"
  },
  "dependencies": {
    "@quicktask/core": "workspace:*"
  }
}
