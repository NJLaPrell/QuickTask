{
  "name": "@quicktask/core",
  "version": "0.2.5",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NJLaPrell/QuickTask.git"
  },
  "type": "module",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "pnpm run build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/**/*.test.mjs",
    "clean": "rm -rf dist"
  }
}
